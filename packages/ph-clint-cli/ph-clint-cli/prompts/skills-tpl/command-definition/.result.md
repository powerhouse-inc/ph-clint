## Expected Outcome

Command is defined with typed inputSchema, exported, and registered in `defineCli`. Help text is complete (every field has `.describe()`). If intended as an agent tool, returns `{ text, data }`. The command handles all input combinations correctly and follows ph-clint conventions (no `console.log`, no `process.exit`, errors are thrown).
