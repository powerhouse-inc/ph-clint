/**
 * Mastra Studio entry point for `mastra dev` / `mastra build` / `mastra start`.
 *
 * Populated by codegen when `features.mastra.enabled`. Until then this file
 * exports nothing — the `mastra:*` scripts will fail gracefully if invoked
 * with Mastra disabled.
 *
 * See examples/05-ph-rupert/src/mastra/index.ts for the enabled form.
 */

// @clint:begin mastra-index
export {};
// @clint:end mastra-index
