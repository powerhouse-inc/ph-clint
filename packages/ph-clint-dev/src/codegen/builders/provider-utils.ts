/**
 * Provider constant used to identify the built-in demo/placeholder model.
 * Models with this provider prefix (e.g. `clint/demo-agent`) do not correspond
 * to a real AI SDK provider and should be excluded from API key generation,
 * provider imports, and env var bridging.
 */
export const DEMO_PROVIDER = 'clint';
